import { OrderTodoPipe } from './order-todo.pipe';

describe('OrderTodoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderTodoPipe();
    expect(pipe).toBeTruthy();
  });
});
